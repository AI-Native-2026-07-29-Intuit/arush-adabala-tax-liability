# Log4j2 startup trimming for a Lambda (W5 D4). Each of these switches off a subsystem that
# costs INIT time and does nothing in a function that lives for one invocation burst.
log4j2.disableJmx=true
log4j2.shutdownHookEnabled=false
log4j2.isWebapp=false
