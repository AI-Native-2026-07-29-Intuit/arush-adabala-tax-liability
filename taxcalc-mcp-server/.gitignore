.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
dist/
# The fixture-replay report is a per-run artefact; the CI gate compares it against the previous
# run's, which comes from the workflow's cache, not from the tree.
.replay/
